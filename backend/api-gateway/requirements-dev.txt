# Дополнительно к requirements.txt — локальные проверки как в CI (lint, полный pytest).
# Установка: pip install -r requirements.txt -r requirements-dev.txt
flake8>=7,<8
pytest>=8,<9
pytest-asyncio>=1.3.0
httpx
aiosqlite
